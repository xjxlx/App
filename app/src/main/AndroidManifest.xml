<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.app">

    <uses-permission
        android:name="android.permission.AUTHENTICATE_ACCOUNTS"
        android:maxSdkVersion="22" /> <!-- 添加帐户需要权限 -->
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" /> <!-- 查看帐户需要权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" /> <!-- // 开机启动权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- 白名单的权限 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> <!-- 添加账户需要的权限 -->
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" /> <!-- 蓝牙的权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" /> <!-- 请求连接和传送数据 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" /> <!-- 启动设备、发现或进行蓝牙设置 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_PRIVILEGED"
        tools:ignore="ProtectedPermissions" /> <!-- bugly权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" /> <!-- 拍照 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" /> <!-- 高德地图的权限 -->
    <!-- 允许访问网络，必选权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 允许获取粗略位置，若用GPS实现定位小蓝点功能则必选 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 允许获取设备和运营商信息，用于问题排查和网络定位，若无gps但仍需实现定位小蓝点功能则此权限必选 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 允许获取网络状态，用于网络定位，若无gps但仍需实现定位小蓝点功能则此权限必选 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 允许获取wifi网络信息，用于网络定位，若无gps但仍需实现定位小蓝点功能则此权限必选 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 允许获取wifi状态改变，用于网络定位，若无gps但仍需实现定位小蓝点功能则此权限必选 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 允许写入扩展存储，用于数据缓存，若无此权限则写到私有目录 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 允许写设备缓存，用于问题排查 -->
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" /> <!-- 允许读设备等信息，用于问题排查 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 高德搜索SDK -->
    <!-- 允许访问网络，必选权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 允许读设备等信息，用于问题排查 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 允许获取网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 允许获取wifi网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 允许写入扩展存储，用于搜索结果缓存，若无此权限则写到私有目录 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 允许写设备缓存，用于问题排查 -->
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" /> <!-- 允许读设备等信息，用于问题排查 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 高德定位权限 -->
    <!-- 用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 用于获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 用于访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 用于写入缓存数据到扩展存储卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 用于申请调用A-GPS模块 -->
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" /> <!-- 如果设置了target >= 28 如果需要启动后台定位则必须声明这个权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- 如果您的应用需要后台定位权限，且有可能运行在Android Q设备上,并且设置了target>28，必须增加这个权限声明 -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".app.App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppThem"
        tools:targetApi="n">
        <activity android:name=".ui.activity.kotlin.KotListActivity"></activity>
        <activity android:name=".ui.activity.kotlin.kotArrayActivity" />
        <activity android:name=".ui.activity.widget.CustomChargingActivity" />
        <activity android:name=".ui.activity.widget.PageListActivity" />
        <activity android:name=".test.TestActivityActivity" />
        <activity android:name=".ui.activity.widget.CustomRound2Activity" />
        <activity android:name=".test.TestUtilActivity" />

        <service
            android:name=".ui.activity.personal.NotificationService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".ui.activity.personal.MapService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ui.activity.personal.SearchMapActivity" />
        <activity
            android:name=".ui.activity.personal.RouseDingDingActivity"
            android:launchMode="singleTop" />
        <activity android:name=".test.AdapterActivity" />
        <activity android:name=".test.ActivityOrFragmentLifecycleActivity" />
        <activity android:name=".ui.activity.kotlin.KotlinMap" />
        <activity android:name=".ui.activity.otherutils.LocationActivity" /> <!-- 账号拉活的                        开始 -->
        <service
            android:name="com.android.helper.utils.account.AccountService"
            android:enabled="true"
            android:exported="true"
            android:label="1:账号注册的服务类，这个是必须实现的">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/account_authenticator" />
        </service>

        <provider
            android:name="com.android.helper.utils.account.SyncProvider"
            android:authorities="com.android.app.account.authority"
            android:label="2:使用provider用来同步账号之间的数据" />

        <service
            android:name="com.android.helper.utils.account.SyncService"
            android:enabled="true"
            android:exported="true"
            android:label="3：账号拉货的数据接收服务">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>
            <!-- 此处的authorities 必须和string中的account_authority数据保持一致 -->
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/sync_adapter" />
        </service> <!-- 此处的authorities 必须和string中的account_authority数据保持一致 -->
        <receiver
            android:name="com.android.account.LifecycleReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.app.lifecycle" />
            </intent-filter>
        </receiver> <!-- 账号拉活的                        结束 -->
        <service
            android:name=".app.Keepalive.AppLifecycleService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".app.Keepalive.AppLifecycleActivity"
            android:label="单渠道保活"
            android:launchMode="singleTop" />

        <receiver
            android:name=".app.Keepalive.keep.keepReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".app.Keepalive.keep.KeepActivity"
            android:excludeFromRecents="true"
            android:theme="@style/KeepTheme" />

        <service
            android:name=".app.Keepalive.AppJobService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <activity android:name=".ui.activity.jetpack.paging.PagingActivity" />
        <activity android:name=".ui.activity.jetpack.navigation.navigation2.Navigation2Activity" />
        <activity android:name=".ui.activity.jetpack.navigation.NavigationActivity" />
        <activity android:name=".ui.activity.jetpack.room.room2.Room2Activity" />
        <activity android:name=".ui.activity.jetpack.room.room1.RoomActivity" />
        <activity android:name=".ui.activity.jetpack.model.ViewModelActivity" />
        <activity
            android:name=".test.TestTouchActivity"
            android:label="手势事件" />
        <activity android:name=".ui.activity.jetpack.lifecycle.LifecycleActivity" />
        <activity android:name=".ui.activity.jetpack.JetPackMapActivity" />
        <activity android:name=".ui.activity.widget.NameListActivity" />
        <activity android:name=".ui.activity.otherutils.AudioPlayerActivity" />
        <activity android:name=".test.TestWebViewActivity" />
        <activity android:name=".test.TestWebSocketActivity" />
        <activity android:name=".test.SlidingMenuActivity" />
        <activity android:name=".test.MoreAdapterActivity" />
        <activity android:name=".test.TestScrollHelperActivity" />
        <activity android:name=".test.AndroidJSActivity" />
        <activity android:name=".ui.activity.hmview.HmCustomViewActivity" />
        <activity android:name=".ui.activity.widget.ScrollPageViewActivity" />
        <activity android:name=".ui.activity.animation.ViewPagerActivity" />
        <activity android:name=".ui.activity.widget.NestSlidingViewActivity" />
        <activity android:name=".ui.activity.widget.InputPassWordActivity" />
        <activity android:name=".ui.activity.widget.TouchUnlockActivity" />
        <activity android:name=".ui.activity.widget.ProgressActivity" />
        <activity android:name=".ui.activity.animation.RadiationAnimationActivity" />
        <activity android:name=".ui.activity.animation.GifViewActivity" />
        <activity android:name=".ui.activity.animation.AnimationMapActivity" />
        <activity
            android:name=".ui.activity.widget.JointActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".ui.activity.widget.RandomActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".ui.activity.widget.CustomRoundImageActivity" />
        <activity android:name=".ui.activity.widget.CustomTestActivity" />
        <activity android:name=".ui.activity.widget.MultipleListViewActivity" />
        <activity android:name=".test.FlexBoxLayoutActivity" />
        <activity android:name=".test.ExpandableActivity" />
        <activity android:name=".test.TestHandlerActivity" />
        <activity android:name=".test.TestMapActivity" />
        <activity android:name=".ui.activity.SendSmsActivity" />
        <activity android:name=".ui.activity.SelectorImageActivity" />
        <activity android:name=".ui.activity.DownLoadListActivity" />
        <activity android:name=".ui.activity.UploadingActivity" />
        <activity android:name=".ui.activity.java.JavaMapActivity" />
        <activity android:name=".ui.activity.java.TestJavaLifeActivity" />
        <activity android:name=".ui.activity.RxJava2Activity" />
        <activity android:name=".ui.activity.WriteXmlTitleActivity" />
        <activity android:name=".ui.activity.ReceiveMapResultTitleActivity">

            <!-- 处理文字的Intent 我们需要定义action,  category, 和文字对应的MIME -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/*" />
            </intent-filter>

            <!-- 处理单张图片的Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
            </intent-filter>

            <!-- 处理多张图片的Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.QywxService"
            android:enabled="true"
            android:exported="true"
            android:label="微信消息的监听"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity android:name=".ui.activity.WorkWxTitleActivity" />

        <service
            android:name=".services.MyService2"
            android:label="server2"
            android:process=":server2" />
        <service
            android:name=".services.MyService1"
            android:label="server1"
            android:process=":server1" />

        <activity android:name=".ui.activity.DemoMapTitleActivity" />
        <activity android:name=".ui.activity.jetpack.livedata.LiveDataActivity" />
        <activity android:name=".ui.activity.H5StartNativeTitleActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="launch"
                    android:scheme="xjx.app.helper" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="abc"
                    android:scheme="http" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".receivers.AppTopTaskReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="TODO"
            android:process=":remote"
            tools:ignore="ExportedReceiver">
            <intent-filter android:priority="1000">
                <action android:name="com.bqxny.zhgj.app.info.task.top" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ui.activity.UnLockTitleActivity"
            android:launchMode="singleInstance" />

        <service
            android:name=".services.LookDogService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".test.ControlAppTitleActivity" />
        <activity android:name=".ui.activity.FaceVideoPlayerTitleActivity" />
        <activity android:name=".ui.activity.FaceAuthenticationTitleActivity" />
        <activity android:name=".ui.activity.widget.ViewMapTitleActivity" />
        <activity android:name=".ui.activity.widget.ProgressTitleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            android:label="7.0临时访问目录权限的处理">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider> <!-- 音乐播放器的服务 -->
        <service
            android:name="com.android.helper.utils.media.audio.AudioService"
            android:enabled="true"
            android:exported="false" /> <!-- 多渠道打包的key -->
        <meta-data
            android:name="CHANNEL_VALUE"
            android:value="${channel_value}" /> <!-- 高德地图 -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="62cf7cf7dae35f5b7093096d2c5b2c53" />

        <service
            android:name="com.amap.api.location.APSService"
            android:foregroundServiceType="location"
            android:label="Android 11 定位需要的服务" />
    </application>

</manifest>